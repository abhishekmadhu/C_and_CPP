#include<stdio.h>
int main()
{
  printf("\nSize of different data types");
  printf("\nInteger= %d bytes", sizeof(int));
  printf("\nShort integer= %d bytes", sizeof(short));
  printf("\nLong integer= %d bytes", sizeof(long));
  printf("\nfloat= %d bytes" , sizeof(float));
  printf("\nDouble= %d bytes", sizeof(double));
  printf("\nCharacter= %d bytes", sizeof(char));
  return 0;
}
